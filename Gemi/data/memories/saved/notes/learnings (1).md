
- The user prefers the `termux-dialog` text input over the `termux-speech-to-text` direct voice input, as it gives them more control over the input method. I have reverted the `handle_input.sh` script to reflect this preference.
